(A) 2017-09-09 In case there's an exception in downloading a pictures, handle the exception by removing (if exists) the partial file to continue
x (A) 2017-09-05 Create the pictures folder if doesn't exist or add it as custom path to parameters
x (A) 2017-08-22 Add exception handling for downloading loop (e.g. -"The download quota for this file has been exceeded") 
